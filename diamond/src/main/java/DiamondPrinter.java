import java.util.List;

class DiamondPrinter {

    List<String> printToList(char letter) {
        int letterValue = 0;
        int numRows = 0;
        List<String> diamondOutput = new List<String>;

        if (!isLetter(letter)) {
            throw new IllegalArgumentException("Must enter a single letter A-Z");
        }

        letterValue = toUpperCase(letter) - 40;
        nuwRows = (letterValue * 2) - 1;

        for (rowNum = 1; rowNum = (numRows + 1)/2; rowNum++ ) {

        }




    }

}
